# Data directory

Place the provided `kc_house_data.csv` (or the supplied dataset file) in this folder.

You can run `python scripts/prepare_data.py --source /path/to/kc_house_dataset.csv` from the repository root to copy the dataset here with the expected filename.
